import { QueueProvider } from "../../models/queueProvider";
import { QueueType } from "../../models/queueType";
import { Factory } from "./factory";

export class QueueProviderFactory extends Factory<QueueType, QueueProvider> {
  
}